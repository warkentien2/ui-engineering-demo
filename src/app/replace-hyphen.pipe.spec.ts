import { ReplaceHyphenPipe } from './replace-hyphen.pipe';

describe('ReplaceHyphenPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceHyphenPipe();
    expect(pipe).toBeTruthy();
  });
});
